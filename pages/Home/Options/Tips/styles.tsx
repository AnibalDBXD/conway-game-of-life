import styled from "styled-components";
import { COLORS } from "../../../../common/styles";

export const StyledTips = styled.ul`
  list-style: none;
  padding: 0;
  margin: 0;
`;

export const StyledPatternType = styled.h2`
  border-bottom: 1px solid ${COLORS.BLACK};
  text-align: center;
  padding-bottom: 8px;
`;
